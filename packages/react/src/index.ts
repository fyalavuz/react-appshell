// Components will be added in subsequent tasks
// For now, export types to verify build works

export type {
  HeaderBehavior,
  HeaderTheme,
  HeaderProps,
  FooterVariant,
  FooterBehavior,
  FooterPosition,
  FooterProps,
  FooterItemProps,
  SafeAreaEdge,
  SafeAreaProps,
  ContentProps,
  AppShellProps,
  AppShellContextValue,
  ScrollDirection,
} from "./types";
